const intent = process.argv[2] || "dev";
const message = `SCANNER APP PLACEHOLDER\nThis workspace is waiting for a React Native implementation.\nNext steps: set up an Expo/React Native app under apps/scanner-app and wire npm scripts.\nCurrently the runner received the intent: ${intent}.`;
console.log(message);
